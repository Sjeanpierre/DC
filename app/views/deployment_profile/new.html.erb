<div class="container">
  <%= simple_form_for :deployment_profile, :url => "/#{@_request.filtered_parameters['rsaccount']}/deployment_profile/select_inputs", :html => {:class => 'form-horizontal' } do |f|  %>
    <%=	 f.input :rs_array, :as => :grouped_select, :collection => @grouped_arrays, :group_method => :last, :group_label_method => :first, :label => 'Rightscale Array', :input_html => {:class => 'chzn-select', :style => 'width: 500px'} %>
    <%= f.button :submit, 'Next' %>
  <% end %>
</div>